// Tour model placeholder
export class Tour {
  constructor({ id, destination, location, price, duration }) {
    this.id = id;
    this.destination = destination;
    this.location = location;
    this.price = price;
    this.duration = duration;
  }
}
