// Booking controller placeholder
export function createBooking(req, res) {
  res.json({ message: 'Booking creation placeholder' });
}

export function getBookings(req, res) {
  res.json({ message: 'Booking list placeholder' });
}
